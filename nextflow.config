profiles {
  uppmax {
      process {
          shell = ['/bin/bash', '-euo', 'pipefail']
          executor = 'slurm'
          clusterOptions = { "-A $params.project" }
          cpus = 8
          memory = '32G'
          time = '5h'
      }
  }
}
